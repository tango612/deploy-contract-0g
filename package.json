{
  "name": "hardhat-galileo-codespace",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "node scripts/deploy.js",
    "compile": "hardhat compile",
    "deploy": "node scripts/deploy.js",
    "test": "hardhat test"
  },
  "devDependencies": {
    "hardhat": "^2.17.0",
    "@nomiclabs/hardhat-ethers": "^2.2.2",
    "ethers": "^5.7.2",
    "dotenv": "^16.0.0"
  }
}
